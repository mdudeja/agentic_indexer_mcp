{
  "name": "agentic-indexer",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "start": "bun run --env-file .env index.ts serve",
    "index": "bun run --env-file .env index.ts index",
    "index-file": "bun run --env-file .env index.ts index-file --file /home/md/Projects/nvim_plugins/agentic_indexer_mcp/src/database/IndexerDB.ts",
    "remove-docstrings": "bun run --env-file .env index.ts remove-docstrings",
    "query": "bun run --env-file .env index.ts query",
    "inspect": "mcp-inspector bun run --env-file .env index.ts serve",
    "lint": "bun tsc --noEmit",
    "test": "bun test"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.27.1",
    "chokidar": "^5.0.0",
    "drizzle-orm": "^1.0.0-beta.16-ea816b6",
    "temporal-polyfill": "^0.3.2",
    "tree-sitter-wasms": "^0.1.13",
    "ts-morph": "^28.0.0",
    "typebox": "^1.1.5",
    "web-tree-sitter": "0.25.3",
    "zod": "^4.3.6"
  },
  "devDependencies": {
    "@modelcontextprotocol/inspector": "^0.21.2",
    "@types/bun": "^1.3.10",
    "bun-types": "^1.3.12",
    "drizzle-kit": "^1.0.0-beta.16-ea816b6",
    "typescript": "^6.0.3"
  }
}
